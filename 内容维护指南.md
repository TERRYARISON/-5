# 内容维护指南（给郑超）

这个网站是「数据驱动」的：**所有你以后会改的内容，都集中放在 `src/content/` 文件夹里的 5 个文本文件中**，每个文件顶部都有中文注释。改文字、加小说、换链接 —— 只改这些文件，不用碰任何页面代码。

## 五个配置文件

| 文件 | 管什么 |
|---|---|
| `src/content/site.ts` | 导航菜单、页底环线顺序、GitHub/邮箱链接、你的名字 |
| `src/content/profile.ts` | 个人简介、简历时间线、证书、精选案例 |
| `src/content/novels.ts` | 小说：封面、状态、一句话简介、完整简介、标签 |
| `src/content/amulet.ts` | 泰瑞堂：主介绍、展示图+一句话说明、交流入口文案 |
| `src/content/apps.ts` | APP：概念图、状态、描述、GitHub 仓库地址 |

## 常见操作

### 加一本新小说
打开 `src/content/novels.ts`，照抄任意一条 `{ ... }`，改成新书的内容：
- `cover: '/novel-新书.jpg'`（把封面图放进 `public/` 文件夹，jpg 格式，宽度约 1024px）
- 暂时没有封面就写 `cover: null`，页面会自动显示「封面绘制中」占位卡

### 加一段工作经历
打开 `src/content/profile.ts`，在 `TIMELINE` 数组最前面照抄一条改内容。

### 换一个 APP / 上线了新 APP
打开 `src/content/apps.ts`：改 `status: '已上线'`，填上 `repo` 仓库地址和 `conceptImg` 概念图。

### 佛牌展示位加图
把照片放进 `public/`，然后在 `src/content/amulet.ts` 的 `AMULET_GALLERY` 里加一行：
`{ img: '/照片文件名.jpg', caption: '一句话说明' },`

### 改邮箱或 GitHub 地址
只改 `src/content/site.ts` 里的 `SOCIALS`，全站（导航、页脚、联系页、所有"写信给我"按钮）会一起更新。

### 改三语按钮的文字（中 / EN / ไทย）
界面上的按钮、导航、提示语有三语翻译，集中在 `src/i18n.tsx` 的 `DICT` 字典里。找到对应的那一行，改 `zh`（中文）、`en`（英文）、`th`（泰文）三个值即可。正文长文（简介、小说介绍等）保持中文为主，不在这个字典里。

## 图片放哪里
所有图片/视频都放在 `public/` 文件夹，配置里用 `/文件名` 引用（注意开头的斜杠）。

## 改完怎么生效
改完文件保存后，需要重新构建发布（找 Kimi 或会代码的朋友跑一句 `npm run build` 即可）。图片建议：jpg、单张 500KB 以内、宽度 1024–1600px。

---
*技术备忘：本网站为纯前端静态站点（React + Vite），没有数据库后台——好处是永远免费托管、不会被黑、加载飞快；代价是改内容需要改文件后重新构建。上面的流程已经把这件事压缩到"只改一个文本文件"。*
